package sys.pro;

/** Qualification work descriptor class. */
class QualificationWork extends GradedSubject {
    /** Construct new qualification work. */
    QualificationWork(Grade grade) {
        super("ВКР", grade);
    }
}
